# For contributors — builds from local source instead of GitHub.
#
# Usage (from hub/ directory):
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up --build -d

services:
  hub:
    build:
      context: .
